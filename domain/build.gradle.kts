// Domain module - Pure Kotlin, no external dependencies (Spring, JPA, etc.)
// Contains business logic, domain models, and interfaces

plugins {
    alias(libs.plugins.kotlin.jvm)
}

dependencies {
    // No Spring or JPA dependencies - pure domain logic
    // Only Kotlin standard library (inherited from root)
}
